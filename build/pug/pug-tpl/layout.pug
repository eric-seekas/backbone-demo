//- index.pug

-var iosUrl = '../vendor/Framework7/css/framework7.ios.css';
-var iosUrlColor = '../vendor/Framework7/css/framework7.ios.colors.css';
-var materialUrl = '../vendor/Framework7/css/framework7.material.css';
-var materialColorUrl = '../vendor/Framework7/css/framework7.material.colors.css';
-var styleUrl = '../dist/css/style.css';
-var backboneUrl = '../vendor/backbone/backbone.js';
-var underscoreUrl = '../vendor/underscore/underscore.js';
-var Framework7Url = '../vendor/Framework7/js/framework7.custom.js';
-var FrameworkIconUrl = '../vendor/Framework7/css/framework7-icons.css';

doctype html
html
    head
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no, minimal-ui">
        <meta name="apple-mobile-web-app-capable" content="yes">
        block cssSrc
            link(rel='stylesheet', href=iosUrl)
            link(rel='stylesheet', href=iosUrlColor)
            link(rel='stylesheet', href=FrameworkIconUrl)
            link(rel='stylesheet', href=styleUrl)
        //link(rel='stylesheet', href=materialUrl)
        block title
            title Default title
    body
        block content
            p Default content
        block jsSrc
            script(src=underscoreUrl)
            script(src=backboneUrl)
            script(src=Framework7Url)
